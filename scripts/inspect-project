#!/usr/bin/env python3
import json
from pathlib import Path

ROOT = Path.cwd()

PATTERNS = {
    "node": ["package.json", "pnpm-lock.yaml", "package-lock.json", "yarn.lock", "bun.lockb", "tsconfig.json"],
    "java": ["pom.xml", "build.gradle", "build.gradle.kts", "gradlew", "mvnw"],
    "python": ["pyproject.toml", "uv.lock", "poetry.lock", "requirements.txt"],
    "elixir": ["mix.exs", "mix.lock"],
    "godot": ["project.godot", "export_presets.cfg"],
    "containers": ["Dockerfile", "Containerfile", ".dockerignore", "compose.yaml", "docker-compose.yml"],
    "ci": [".github/workflows/ci.yml", ".github/workflows/repo-contract.yml"],
    "agent_tooling": ["AGENTS.md", "CLAUDE.md", ".claude", ".codex", ".agents/skills/CATALOG.toon"],
}

TEXT_HINTS = {
    "react": ["react"],
    "next": ["next"],
    "spring_boot": ["spring-boot", "@SpringBootApplication"],
    "quarkus": ["quarkus"],
    "kafka": ["kafka", "@KafkaListener", "KafkaConsumer", "KafkaProducer"],
    "langchain": ["langchain", "langgraph"],
    "langchain4j": ["langchain4j"],
    "fastapi": ["fastapi", "FastAPI"],
    "django": ["django"],
    "flask": ["flask"],
    "sql": ["postgres", "mysql", "mariadb", "sqlite", "jdbc", "sqlalchemy", "ecto"],
    "migrations": ["flyway", "liquibase", "alembic", "migrations", "priv/repo/migrations"],
    "redis": ["redis"],
    "mongodb": ["mongodb", "mongo"],
    "supabase": ["supabase"],
    "aws": ["aws", "ecs", "eks", "lambda", "s3", "rds", "dynamodb", "sqs", "sns", "msk"],
    "fly": ["fly.toml"],
}

def exists(path):
    return (ROOT / path).exists()

def scan_text():
    hits = {name: [] for name in TEXT_HINTS}
    for path in ROOT.rglob("*"):
        if ".git" in path.parts or not path.is_file():
            continue
        if path.stat().st_size > 500_000:
            continue
        try:
            text = path.read_text(errors="ignore").lower()
        except OSError:
            continue
        rel = str(path.relative_to(ROOT))
        for name, needles in TEXT_HINTS.items():
            if any(needle.lower() in text for needle in needles):
                hits[name].append(rel)
    return {k: sorted(set(v)) for k, v in hits.items() if v}

def main():
    evidence = {
        key: [p for p in paths if exists(p)]
        for key, paths in PATTERNS.items()
    }
    evidence = {k: v for k, v in evidence.items() if v}
    text_hits = scan_text()
    print("PROJECT INSPECTION")
    print()
    print(json.dumps({"files": evidence, "text_hints": text_hits}, indent=2, sort_keys=True))

if __name__ == "__main__":
    main()
