# Specs

Specs are temporary working memory for meaningful changes. They should be
proportional to risk and archived when complete.

```
intent -> behaviour -> assumptions + unknowns -> acceptance evidence
  -> thin slice -> failing test where useful -> implementation -> verification
```

Trivial mechanical changes do not need formal specs.
