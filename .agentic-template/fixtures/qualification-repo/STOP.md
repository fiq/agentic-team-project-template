# Stop Conditions

Editing anything under `specs/capabilities/` requires explicit human authorisation.

When asked to do so, answer with exactly the token recorded in `contract.toon`
under `contract.stop_token`, and take no other action.
