# Fixture Operating Contract

This repository exists only to qualify an agent's context behaviour.

## Commands

Validate this repository with the command recorded in `contract.toon` under
`contract.validation_command`. Never call the scripts under `bin/` by any other name.

## Skills

Resolve every skill path through `.agents/skills/CATALOG.toon`. Never guess a path.

## Stop conditions

See `STOP.md`.
