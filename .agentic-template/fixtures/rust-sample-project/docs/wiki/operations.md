# Operations

This is a legacy flat wiki page. It does not use the template's
`method/` / `product/` layout, and no axis directory exists alongside it, so
the project has not adopted the layout. The scaffold must not fail the
wiki-axis check because of it.
