# Ideate — Failure Modes

- Running more than ~4 rounds or debating trivial mechanical changes.
- Pretending sequential `/sudo` passes are independent review.
- Leaving the change proposal unvalidated.
- Forcing consensus when personas genuinely disagree — the lead owns synthesis.
