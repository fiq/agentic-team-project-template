# Tooling Policy

Nix owns developer tooling. Containers package deployable applications when
appropriate. Compose describes local runtime topology only when needed.

Superpowers and MCP integrations are detected, not assumed. Do not modify
global tooling or expose secrets without explicit permission.
